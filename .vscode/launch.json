{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Dune",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/dune/main.go",
            "cwd": "${workspaceFolder}"
        }
    ]
}